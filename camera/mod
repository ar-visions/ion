dep(vulkan:vkh)
cpp(17)

if(APPLE)
    dep(IOSurface QuartzCore Metal CoreMedia CoreFoundation Cocoa IOKit AVFoundation)
    cflags(+-fobjc-arc)
else()
    
    print("src is ${_src}")

    src(-/home/kalen/src/ion/camera/apple.mm)

    print("src is ${_src}")
endif()