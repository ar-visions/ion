if(APPLE)
    dep(IOSurface QuartzCore Metal AppKit CoreGraphics CoreFoundation Cocoa IOKit)
elseif(LINUX)
    dep(X11)
endif()

dep(vulkan:vk @Vulkan vulkan:vkh mx:async ion:net ion:webrtc ion:media ion:camera skia:skia bzip2:bz2_static)

includes(${INSTALL_PREFIX}/../extern/skia)
includes(${INSTALL_PREFIX}/include/skia)