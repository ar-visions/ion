dep(mx:async ion:net ion:media ion:camera vulkan:vkh harfbuzz:harfbuzz skia:skia freetype:freetyped bzip2:bz2_static)

if(APPLE)
    dep(IOSurface QuartzCore Metal AppKit CoreGraphics CoreFoundation Cocoa IOKit)
else()
    dep(X11)
endif()

# skia headers are incredibly lame, and reference their own 'include' path hard coded. simply not a library.
# they also reference their own basic source root.
includes(${INSTALL_PREFIX}/../extern/skia-m112)
includes(${INSTALL_PREFIX}/include/skia)

cpp(17)
