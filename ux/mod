if(APPLE)
    dep(IOSurface QuartzCore Metal AppKit CoreGraphics CoreFoundation Cocoa IOKit)
elseif(LINUX)
    dep(X11)
elseif(WIN32)
    dep(msvcrt)
    cxxflags(+/bigobj)
endif()


dep(ion:canvas @Vulkan mx:async ion:regex ion:net ion:ssh ion:media
    skia:svg      skia:icu          skia:expat    skia:pathkit 
    skia:sksg     skia:skresources  skia:skottie  skia:skia
    skia:skshaper skia:skunicode)

includes(${EXTERN_DIR}/skia)
includes(${EXTERN_DIR}/oniguruma/src)