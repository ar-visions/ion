dep(mx:async
    opusenc:opusenc
    opusfile:opusfile
    opus:opus
    ogg:ogg
    shine:shine
    speexdsp:speexdsp)

if(WIN32)
    dep(libpng:libpng16_static
        zlib:zlibstatic)
else()
    dep(libpng:png zlib:z)
endif()

includes(${INSTALL_PREFIX}/include/opus)
