if(APPLE)
    skip()
else()
    cpp(17)
    dep(dl pthread)
    skip()
    #includes(${EXTERN_DIR}/vk_video_samples/vk_video_encoder
    #         ${EXTERN_DIR}/vk_video_samples/common/include)
    
    src(${EXTERN_DIR}/vk_video_samples/vk_video_encoder/NvEncodeApp.cpp)
endif()
